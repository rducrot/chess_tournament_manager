class Player:

    def __int__(self, last_name, first_name,
                date_of_birth, gender, rank):
        self.last_name = last_name
        self.first_name = first_name
        self.date_of_birth = date_of_birth
        self.gender = gender
        self.rank = rank
